diff -ruN a/dep/CascLib/src/libtomcrypt/src/headers/tomcrypt_custom.h b/dep/CascLib/src/libtomcrypt/src/headers/tomcrypt_custom.h
--- a/dep/CascLib/src/libtomcrypt/src/headers/tomcrypt_custom.h	2026-07-04 12:44:54.980655463 +0000
+++ b/dep/CascLib/src/libtomcrypt/src/headers/tomcrypt_custom.h	2026-07-04 12:45:07.444656057 +0000
@@ -19,6 +19,14 @@
 #define USE_LTM
 #define LTM_DESC
 
+/* explicit prototypes: some libc headers define memset/memcpy as macros,
+   which triggers LTC_NO_PROTOTYPES and would leave these undeclared */
+#include <stddef.h>
+void * LibTomMalloc(size_t n);
+void * LibTomRealloc(void *p, size_t n);
+void * LibTomCalloc(size_t n, size_t s);
+void LibTomFree(void *p);
+
 /* macros for various libc functions you can change for embedded targets */
 #ifndef XMALLOC
    #ifdef malloc 
diff -ruN a/dep/g3dlite/include/G3D/AtomicInt32.h b/dep/g3dlite/include/G3D/AtomicInt32.h
--- a/dep/g3dlite/include/G3D/AtomicInt32.h	2026-07-04 12:32:03.272618665 +0000
+++ b/dep/g3dlite/include/G3D/AtomicInt32.h	2026-07-04 12:32:23.892619648 +0000
@@ -76,12 +76,16 @@
 
 #       elif defined(G3D_LINUX) || defined(G3D_FREEBSD)
 
+#           if defined(__i386__) || defined(__x86_64__)
             int32 old;
             asm volatile ("lock; xaddl %0,%1"
                   : "=r"(old), "=m"(m_value) /* outputs */
                   : "0"(x), "m"(m_value)   /* inputs */
                   : "memory", "cc");
             return old;
+#           else
+            return __sync_fetch_and_add(&m_value, x);
+#           endif
             
 #       elif defined(G3D_OSX)
 
@@ -115,6 +119,7 @@
             // Note: returns the newly decremented value
             return InterlockedDecrement(&m_value);
 #       elif defined(G3D_LINUX)  || defined(G3D_FREEBSD)
+#           if defined(__i386__) || defined(__x86_64__)
             unsigned char nz;
 
             asm volatile ("lock; decl %1;\n\t"
@@ -123,6 +128,9 @@
                           : "m" (m_value)
                           : "memory", "cc");
             return nz;
+#           else
+            return __sync_sub_and_fetch(&m_value, 1) != 0 ? 1 : 0;
+#           endif
 #       elif defined(G3D_OSX)
             // Note: returns the newly decremented value
             return OSAtomicDecrement32(&m_value);
@@ -145,12 +153,16 @@
 #       elif defined(G3D_LINUX) || defined(G3D_FREEBSD) || defined(G3D_OSX)
             // Based on Apache Portable Runtime
             // http://koders.com/c/fid3B6631EE94542CDBAA03E822CA780CBA1B024822.aspx
+#           if defined(__i386__) || defined(__x86_64__)
             int32 ret;
             asm volatile ("lock; cmpxchgl %1, %2"
                           : "=a" (ret)
                           : "r" (exchange), "m" (m_value), "0"(comperand)
                           : "memory", "cc");
             return ret;
+#           else
+            return __sync_val_compare_and_swap(&m_value, comperand, exchange);
+#           endif
 
             // Note that OSAtomicCompareAndSwap32 does not return a useful value for us
             // so it can't satisfy the cmpxchgl contract.
diff -ruN a/dep/g3dlite/include/G3D/System.h b/dep/g3dlite/include/G3D/System.h
--- a/dep/g3dlite/include/G3D/System.h	2026-07-04 12:32:03.272618665 +0000
+++ b/dep/g3dlite/include/G3D/System.h	2026-07-04 12:32:34.144620137 +0000
@@ -497,6 +497,7 @@
 #elif defined(G3D_LINUX)
 
     inline uint64 System::getCycleCount() {
+#if defined(__i386__) || defined(__x86_64__)
         uint32 timehi, timelo;
 
         __asm__ __volatile__ (
@@ -506,6 +507,11 @@
             : );
 
         return ((uint64)timehi << 32) + (uint64)timelo;
+#else
+        struct timespec ts;
+        clock_gettime(CLOCK_MONOTONIC, &ts);
+        return (uint64)ts.tv_sec * 1000000000ULL + (uint64)ts.tv_nsec;
+#endif
     }
 
 #elif defined(G3D_OSX)
diff -ruN a/dep/g3dlite/source/FileSystem.cpp b/dep/g3dlite/source/FileSystem.cpp
--- a/dep/g3dlite/source/FileSystem.cpp	2026-07-04 12:36:42.404631975 +0000
+++ b/dep/g3dlite/source/FileSystem.cpp	2026-07-04 12:36:49.304632304 +0000
@@ -36,6 +36,10 @@
 #   include <unistd.h>
 #   define _getcwd getcwd
 #   define _stat stat
+#   ifdef __APPLE__
+        // macOS: stat is always 64-bit; stat64 was removed on Apple Silicon
+#       define stat64 stat
+#   endif
 #   define stricmp strcasecmp 
 #   define strnicmp strncasecmp 
 #endif
diff -ruN a/dep/g3dlite/source/System.cpp b/dep/g3dlite/source/System.cpp
--- a/dep/g3dlite/source/System.cpp	2026-07-04 12:32:59.948621367 +0000
+++ b/dep/g3dlite/source/System.cpp	2026-07-04 12:33:09.736621834 +0000
@@ -1701,7 +1701,11 @@
 // for a discussion of why the second version saves ebx; it allows 32-bit code to compile with the -fPIC option.
 // On 64-bit x86, PIC code has a dedicated rip register for PIC so there is no ebx conflict.
 void System::cpuid(CPUIDFunction func, uint32& eax, uint32& ebx, uint32& ecx, uint32& edx) {
-#if ! defined(__PIC__) || defined(__x86_64__)
+#if ! defined(__i386__) && ! defined(__x86_64__)
+    // No CPUID instruction on non-x86 architectures
+    (void)func;
+    eax = ebx = ecx = edx = 0;
+#elif ! defined(__PIC__) || defined(__x86_64__)
     // AT&T assembler syntax
     asm volatile(
                  "movl $0, %%ecx   \n\n" /* Wipe ecx */
